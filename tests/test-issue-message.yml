name: Test issue_message input
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GITHUB_EVENT_NAME: issues
      GITHUB_REF: refs/heads/main
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          issue_message: "Welcome! Thanks for opening your first issue."
          repo_token: ${{ github.token }}
        continue-on-error: true
      - name: Assert action ran with issue_message input
        run: |
          echo "Action outcome: ${{ steps.action.outcome }}"
          # The action may fail due to no real GitHub API in act, but it should
          # at least start and attempt to run (not fail due to missing input).
          # We verify the step was reached and the input was accepted.
          if [ "${{ steps.action.outcome }}" = "skipped" ]; then
            echo "FAIL: action was skipped unexpectedly"
            exit 1
          fi
          echo "PASS: action ran with issue_message input"
